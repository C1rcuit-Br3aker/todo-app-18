<nav class="nav-container">
  <div class="nav__menu">
    <ul class="todo__menu">
      {{!--Each-loop to grab lists  --}}
      {{#each model as |list|}}
      <li class="todo-title">
        {{!--Will need to link to individual forms  --}}
        {{list.title}}
      </li>
      {{/each}}
    </ul>
    {{!--Links to 'New' page  --}}
    {{#link-to "lists.new"}}
    <p class="add-list">Add New List</p>
    {{/link-to}}</div>
</nav>

{{outlet}}
